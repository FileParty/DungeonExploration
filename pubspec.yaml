name: gemini_survival
description: "제미나이 AI API 제출 프로젝트"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flame: ^1.16.0
  flutter:
    sdk: flutter
  flutter_animate: ^4.5.0
  google_fonts: ^6.1.0
  provider: ^6.0.0
  cupertino_icons: ^1.0.6
  google_generative_ai: ^0.4.4
  flutter_dotenv: ^5.0.0
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  assets:
    - assets/
  uses-material-design: true
